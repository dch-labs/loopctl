[package]
name = "derive-consumer-fixture"
version = "0.0.0"
edition = "2024"
rust-version = "1.98"
publish = false

[workspace]

[dependencies]
lc = { package = "loopctl", path = "../../..", features = ["derive"] }
serde = { version = "1", features = ["derive"] }

[lints.rust]
warnings = "deny"
